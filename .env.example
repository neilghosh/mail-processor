# Mail Processor Environment Variables
# Copy this file to .env and fill in your values
# Or run: ./setup.sh for interactive setup

# ============================================
# Google OAuth Configuration (Required)
# Get these from: https://console.cloud.google.com/
# ============================================
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# ============================================
# Google Cloud Configuration (Local Development Only)
# ============================================
# IMPORTANT: Leave these blank when deploying to Cloud Run!
# Cloud Run automatically provides project ID via GOOGLE_CLOUD_PROJECT env var (ADC)
#
# For local development only:
# 1. Download service account key from Google Cloud Console
# 2. Set GOOGLE_CLOUD_PROJECT_ID to your project ID
# 3. Set GOOGLE_APPLICATION_CREDENTIALS to the path of your service account JSON
#
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ============================================
# Encryption (Generated automatically by setup.sh)
# ============================================
# 256-bit encryption key for storing sensitive tokens
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-256-bit-hex-string-here

# ============================================
# API Security (Generated automatically by setup.sh)
# ============================================
# API key for /api/tasks/process-emails endpoint
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=your-api-key-hex-string-here

# ============================================
# Server Configuration
# ============================================
# Port to run the server on
PORT=8080

# Environment: development, production, staging
NODE_ENV=development

# OAuth Redirect URI (update for production)
# Local: http://localhost:8080/auth/google/callback
# Production: https://yourdomain.com/auth/google/callback
OAUTH_REDIRECT_URI=http://localhost:8080/auth/google/callback

# ============================================
# Gemini API Configuration
# ============================================
# API key for Gemini AI (for PDF analysis)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# ============================================
# PDF Processing Configuration
# ============================================
# PDF password map (JSON format)
# Maps subject keywords to passwords for encrypted PDFs
# Example: {"invoice":"password1","receipt":"password2"}
PDF_PASSWORDS='{"invoice":"password1","receipt":"password2"}'